# Nhật ký kỹ thuật RBL-4 (Technical Notes)

## Mọi quyết định kỹ thuật / Deviation
* Ghi chú lại mọi lần thay đổi so với proposal (nếu có, và phải được GV duyệt).
* Ghi chú thông số Random Seed khi lọc Pilot data (ví dụ: `random_seed = 42`).

## Error Log (Nhật ký lỗi)
* Ghi nhận các lỗi phát sinh trong lúc gọi API (tỷ lệ lỗi, lý do API trả về rỗng).
